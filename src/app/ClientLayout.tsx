'use client';
import { useEffect, useState } from 'react';

export default function ClientLayout({ children }: { children: React.ReactNode }) {
  // Place any client-side logic (hooks, etc.) here if needed in future
  return <>{children}</>;
}
